name: store_version
description: Check if your user has the most recent version of your Flutter app.
version: 0.0.1
homepage: https://github.com/timtraversy/store_version

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  package_info_plus: ^1.4.3
  http: ^0.13.4
  html: ^0.15.0
  url_launcher: ^6.1.5
  collection: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
